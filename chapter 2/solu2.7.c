#include <stdio.h>
int main()
{
    int x=2,y=3;
     int z;
    printf("%hd=%d+%d",z=x+y);
//so when we declare 2 six digit numbers to x and y then the prblm will be with z as
//it's size to store is only(bytes)is 2 and that of int is 4 so,the value will not get stored...


    return 0;
}
