#include <stdio.h>
int main(){
    int a;
    float b;
    long int c;
    short int d;
    double e;
    int a1,a2,a3,a4,a5;
    printf("a1=%d,a2=%d,a3=%d,a4=%d,a5=%d",sizeof(a),sizeof(b),sizeof(c),sizeof(d),sizeof(e));

return 0;
}
